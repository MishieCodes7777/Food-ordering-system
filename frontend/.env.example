VITE_API_URL=http://localhost:5000

# Required for Google sign-in on the login/register pages. Must match the
# backend's GOOGLE_CLIENT_ID. Leave unset to disable the Google button.
# VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here
